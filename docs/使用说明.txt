【批量去水印功能使用说明】

已成功加载文件夹中的第一张图片。

操作步骤：
1. 请在界面上当前显示的图片上，使用画笔绘制出需要去除水印的区域。
   - 鼠标左键点击并拖动进行绘制。
   - 滚动鼠标滚轮可以缩放图片。
   - 按住鼠标中键可以拖动图片。

2. 绘制完成后，点击右侧绿色的【开始去水印】按钮。

3. 程序将自动把您刚刚绘制的蒙版（Mask）应用到该文件夹下的所有图片中，并开始后台处理。

注意事项：
⚠️ 批量处理仅适用于：所有图片具有相同的尺寸、相同的比例，且水印出现在完全相同的位置。
⚠️ 如果图片尺寸不一致，可能会导致水印去除位置偏移或失败。
